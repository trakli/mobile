# ⚠️ TEMPORARY WORKFLOW - DELETE AFTER USE ⚠️
# This workflow outputs the base64 keystore so you can retrieve it locally
# 
# Steps:
# 1. Push this file to GitHub
# 2. Go to Actions tab and run this workflow manually
# 3. Copy the base64 output from the logs
# 4. On your local machine: echo '<base64_string>' | base64 --decode > android/key.jks
# 5. DELETE THIS WORKFLOW FILE after retrieving the keystore

name: 🔓 Temporary - Get Keystore Base64

on:
  workflow_dispatch:  # Allows manual trigger

jobs:
  output-keystore:
    name: Output Keystore Base64
    runs-on: ubuntu-latest
    steps:
      - name: Output Keystore Base64
        run: |
          echo "⚠️  COPY THE BASE64 STRING BELOW ⚠️"
          echo "=================================="
          echo ""
          echo "${{ secrets.ANDROID_KEYSTORE_FILE_BASE64 }}"
          echo ""
          echo "=================================="
          echo "⚠️  To decode locally, run:"
          echo "   echo '<base64_string>' | base64 --decode > android/key.jks"
          echo ""
          echo "⚠️  Then set password:"
          echo "   export ANDROID_KEYSTORE_PASSWORD='${{ secrets.ANDROID_KEYSTORE_PASSWORD }}'"
          echo ""
          echo "⚠️  DELETE THIS WORKFLOW AFTER USE!"

